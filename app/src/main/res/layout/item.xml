<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/suwret"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="@dimen/magin_standart"
        android:layout_marginStart="@dimen/magin_standart"
        android:layout_marginBottom="@dimen/magin_standart"
        android:src="@drawable/user"/>

    <TextView
        android:id="@+id/ati"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/suwret"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="@dimen/magin_standart"
        android:layout_marginTop="@dimen/magin_standart"
        android:text="This title text"
        android:textSize="16sp"
        android:textColor="@android:color/background_dark"/>

    <TextView
        android:id="@+id/familiyasi"
        android:layout_width="wrap_content"
        android:layout_height="20dp"
        app:layout_constraintStart_toEndOf="@id/suwret"
        app:layout_constraintTop_toBottomOf="@id/ati"
        android:layout_marginStart="@dimen/magin_standart"
        android:text="This title text"/>

</androidx.constraintlayout.widget.ConstraintLayout>